largura = float(input('Largura:'))
altura = float(input('Altura:'))
print('Sua área é de {:.1f}m²'.format(largura*altura))
print('Para pintar essa parede, precisará de {} litros de tinta'.format((largura*altura)/2))
